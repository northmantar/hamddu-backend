export enum PointTransactionType {
  EARN = "EARN",
  USE = "USE",
  CANCEL = "CANCEL",
}

export enum PointTransactionStatus {
  COMPLETED = "COMPLETED",
  CANCEL = "CANCEL",
}

export enum PointActionType {
  WATCH = "WATCH",
  CHALLENGE = "CHALLENGE",
  COMMENT = "COMMENT",
}
